#include <stdio.h>
#include <conio.h>

main()
{
	int a, b=0;
	clrscr();

	printf("Enter any integer:");
	scanf("%d",&a);

	do
	{
		a = a/10;
		b++;

	}while (a!=0);

	printf("counct of digit : %d",b);

	getch();
}