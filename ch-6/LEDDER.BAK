#include <stdio.h>
#include <conio.h>

main()
{
	int n;
	clrscr();

	 printf("enter any number");
	 scanf("%d",&n);

	if(n>0)
	{
		printf("positive");
	}
	else if(n<0)
	{
		printf("negetive");
	}
	else if(n==0)
	{
		printf("zero");
	}
	else
	{
		printf("invalid number");
	}
	getch();
}