#include <stdio.h>
#include <conio.h>

main()
{
	int  year = 2000,b,c;

	clrscr();

	printf("enter first number: ");
	scanf("%d",&b);
	printf("enter second number:");
	scanf("%d",&c);

	while(1)
	{
		if(year % 4==0&&year%2!=0)
		{
			printf("%d\n",year);
		}
	  year++;
	}
	getch();

}
