#include <stdio.h>
#include <conio.h>
main()
{
	int n,i,f=1;

	clrscr();

	printf("enter any number:");
	scanf("%d",&n);
		for(i=1;i<=n;i++)
		{
			f=f*i;
		}
		printf("the factorial number %d is %d",n,f);

	getch();
}